import type { AzureCloudTopic } from "./index";

export const topic04: AzureCloudTopic = {"key":"scale-to-zero-cold-starts","route":"azure-scale-to-zero-cold-starts","title":"Scale-to-Zero, Cold Starts, Replicas, CPU, and Memory","shortTitle":"Scale-to-zero and cold starts","summary":"How Azure changes the number of running application copies and what each copy can use.","mentalModel":[{"type":"paragraph","text":"A **replica** is one running copy of a Container App revision."},{"type":"paragraph","text":"If `notes.portfolio.example` has three replicas, Azure is running three separate copies of the notes container. Managed ingress can distribute requests among them."},{"type":"paragraph","text":"**Scale-to-zero** means Azure is allowed to stop all replicas when there is no work. This can reduce compute usage for a low-traffic portfolio demo."},{"type":"paragraph","text":"A **cold start** is the delay when a request arrives while no replica is running. Azure must schedule a replica, pull the image if needed, start the container, wait for the application to become ready, and then route traffic."},{"type":"paragraph","text":"**CPU and memory** are assigned per replica. A setting of `0.5 vCPU` and `1 GiB` does not describe the entire app forever. It describes the resources available to each running replica."}],"flowTitle":"Request flow when a replica is already running","flow":["A visitor opens `https://notes.portfolio.example`.","DNS and HTTPS routing bring the request to Azure Container Apps ingress.","Azure finds an active revision with a ready replica.","Ingress sends the request to that replica.","The application responds."],"sections":[{"title":"Request flow after scale-to-zero","blocks":[{"type":"ordered","items":["A visitor opens the same URL.","Azure ingress receives the request.","The scale rule detects demand while the revision has zero replicas.","Azure schedules a new replica.","Azure obtains the selected image and starts the container.","Startup and readiness checks must succeed.","Azure routes the pending or retried request to the replica.","The application responds."]},{"type":"paragraph","text":"This is the cold path. The user experiences some or all of the startup delay."}]},{"title":"Minimum and maximum replicas","blocks":[{"type":"bullets","items":["**Minimum replicas** is the lowest number Azure should keep running.","**Maximum replicas** is the highest number Azure may create for that revision.","`minReplicas: 0` allows scale-to-zero.","`minReplicas: 1` keeps one warm replica available.","A maximum prevents uncontrolled scale but can also become a bottleneck."]},{"type":"paragraph","text":"For a portfolio demo, zero may be reasonable when cost matters more than first-request speed. For a live interview demonstration, one warm replica can be safer."}]},{"title":"What causes scaling","blocks":[{"type":"paragraph","text":"HTTP applications commonly scale based on concurrent requests. Azure Container Apps also supports TCP and event-driven rules, including rules based on queue depth or other KEDA-supported sources."},{"type":"paragraph","text":"Scaling is not the same as giving one replica more power."},{"type":"bullets","items":["**Scale out** means more replicas.","**Scale in** means fewer replicas.","**Scale up** informally means more CPU or memory per replica, which normally requires a configuration change and new revision."]},{"type":"paragraph","text":"A CPU-heavy single request may not improve merely because more replicas are allowed. It may need more CPU per replica or work split into smaller jobs."}]},{"title":"CPU and memory choices","blocks":[{"type":"paragraph","text":"CPU and memory should reflect actual application behavior."},{"type":"paragraph","text":"Too little memory can cause termination or restart when the process exceeds its limit. Too little CPU can make startup, builds performed at runtime, PDF parsing, or AI preprocessing slow."},{"type":"paragraph","text":"Too much CPU or memory can waste paid allocation when replicas remain active."},{"type":"paragraph","text":"The important operational questions are:"},{"type":"bullets","items":["How long does the application take to start?","What is peak memory during its largest real operation?","Can two requests run safely at once?","Is work CPU-bound, memory-bound, network-bound, or waiting on an external API?","Can the app run in several replicas without relying on local state?"]}]}],"azureManages":[{"title":null,"items":["Starting and stopping replicas according to configured scale rules.","Routing requests to ready replicas.","Replacing failed replicas.","Platform capacity within the selected environment and workload profile.","Collecting replica-level metrics.","Pre-warming new replicas before shifting traffic during normal scaling behavior."]}],"youManage":[{"title":null,"items":["Minimum and maximum replicas.","Scale rules and concurrency thresholds.","CPU and memory assigned to each replica.","Application startup time.","Image size and dependency loading.","Startup and readiness probes.","Safe behavior when multiple replicas run.","External service rate limits.","User expectations around cold starts.","Whether a demo should remain warm before an important presentation."]}],"failures":[{"title":"The first request feels broken","detail":"The app is scaling from zero, but the UI provides no loading state and the browser times out before startup completes."},{"title":"The container is under-provisioned","detail":"The process runs locally with generous resources but exceeds the cloud memory limit or starts very slowly with limited CPU."},{"title":"Maximum replicas is too low","detail":"Traffic reaches the scale threshold, but Azure cannot add enough replicas because the configured maximum is already reached."},{"title":"Concurrency is set without measuring the app","detail":"A replica may receive more simultaneous work than it can safely process, causing long latency or memory spikes."},{"title":"Local state breaks after scaling","detail":"A user uploads a file to replica A, then the next request reaches replica B, where the file does not exist."},{"title":"A background-only app scales to zero and cannot wake","detail":"If ingress is disabled and there is no event-driven scale rule, a zero-replica app has no signal that tells Azure to start it again."},{"title":"A large image increases cold-start time","detail":"A container that downloads browsers, models, or package caches at startup can make the cold path much slower."}],"portal":[{"title":null,"items":["**Container App > Scale** — minimum, maximum, and scale rules.","**Container App > Containers** — CPU and memory per replica.","**Container App > Revisions and replicas** — active revisions and running replicas.","**Container App > Metrics** — replica count, requests, CPU, memory, restarts, and network behavior.","**Container App > Log stream** — startup and runtime output.","**Container App > Ingress** — HTTP exposure and target port.","**Container Apps Environment > Workload profiles** — available compute model and environment capacity."]}],"checklist":["Minimum and maximum replicas match the desired cost and responsiveness tradeoff.","A cold request succeeds from zero replicas within an acceptable user-facing time.","CPU and memory remain below limits during the largest realistic operation.","The application works correctly when two or more replicas handle requests.","Metrics and logs show expected scaling rather than crashes, timeouts, or repeated restarts."],"sources":[{"label":"learn.microsoft.com/azure/container-apps/scale-app","url":"https://learn.microsoft.com/azure/container-apps/scale-app"},{"label":"learn.microsoft.com/azure/container-apps/workload-profiles-overview","url":"https://learn.microsoft.com/azure/container-apps/workload-profiles-overview"},{"label":"learn.microsoft.com/azure/container-apps/revisions","url":"https://learn.microsoft.com/azure/container-apps/revisions"},{"label":"learn.microsoft.com/azure/container-apps/observability","url":"https://learn.microsoft.com/azure/container-apps/observability"},{"label":"learn.microsoft.com/azure/container-apps/quotas","url":"https://learn.microsoft.com/azure/container-apps/quotas"}]};
